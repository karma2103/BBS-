<%- include('../../partials/header'); %>
    <div class="container mt-5">
        <h3 class="mb-4">IT Daily Report</h3>
        <div class="d-flex justify-content-end mb-3">
            <a href="/studio" class="btn btn-primary">Update Studio Work</a>
        </div>
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Today's Events</th>
                    <th>Work Done (Yesterday)</th>
                    <th>Special Comments</th>
                    <th>Air on Equipment</th>
                    <th>Post Production</th>
                </tr>
            </thead>
            <tbody>
                <% if (std.length > 0) { %>
                    <% std.forEach((main, index) => { %>
                        <tr>
                            <td><%= index + 1 %></td>
                            <td><%= main.todaysEvent %></td>
                            <td><%= main.workDone %></td>
                            <td><%= main.specialComment %></td>
                            <td><%= main.AironEquiptment %></td>
                            <td><%= main.postProduction %></td>
                            </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="9" class="text-center">Not Updated.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<%- include('../../partials/footer'); %>